import { createTheme } from '@mui/material/styles';

// eslint-disable-next-line import/prefer-default-export
export const ThemeToggleButton = createTheme({
  palette: {
    primary: {
      main: '#c20014',
    },
  },
});
